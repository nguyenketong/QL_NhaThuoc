@model IEnumerable<dynamic>
@{
    ViewData["Title"] = "Quản lý nhóm thuốc";
}

<div class="d-flex justify-content-end mb-4">
    <a asp-action="Create" class="btn btn-success"><i class="fas fa-plus"></i> Thêm nhóm</a>
</div>

<div class="admin-table">
    <table class="table table-hover">
        <thead>
            <tr>
                <th>ID</th>
                <th>Tên nhóm thuốc</th>
                <th>Mô tả</th>
                <th>Số thuốc</th>
                <th width="150">Thao tác</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var item in Model)
            {
                <tr>
                    <td>@item.NhomThuoc.MaNhomThuoc</td>
                    <td><strong>@item.NhomThuoc.TenNhomThuoc</strong></td>
                    <td>@item.NhomThuoc.MoTa</td>
                    <td><span class="badge bg-info">@item.SoLuongThuoc sản phẩm</span></td>
                    <td>
                        <a asp-action="Edit" asp-route-id="@item.NhomThuoc.MaNhomThuoc" class="btn btn-sm btn-warning">
                            <i class="fas fa-edit"></i>
                        </a>
                        <form asp-action="Delete" asp-route-id="@item.NhomThuoc.MaNhomThuoc" method="post" class="d-inline"
                              onsubmit="return confirm('Xóa nhóm này?')">
                            <button type="submit" class="btn btn-sm btn-danger"><i class="fas fa-trash"></i></button>
                        </form>
                    </td>
                </tr>
            }
        </tbody>
    </table>
</div>
